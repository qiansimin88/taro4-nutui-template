# 分包重构指南

## 📋 重构目标

根据微信小程序分包最佳实践，将现有页面重新组织到科学的分包结构中，实现：
- 主包体积最小化（仅保留 TabBar 页面和入口页面）
- 按业务功能划分分包
- 提升首屏加载速度
- 优化用户体验

## 🏗️ 新的分包架构

### 主包 (pages/)
**原则**：只放 TabBar 页面、入口页面和公共页面
```
src/pages/
├── index/                  # 首页（TabBar）
├── personalCenter/         # 个人中心首页（TabBar）
├── login/                  # 登录页（入口页面）
└── webview/               # WebView（公共页面）
```

### 分包 (sub-packages/)
**原则**：按业务功能划分，互不依赖

```
src/sub-packages/
├── user/                   # 用户相关分包
│   ├── profile/           # 个人资料
│   ├── address/           # 地址管理
│   ├── receivingAccount/  # 收款账户
│   ├── points/            # 积分中心
│   ├── favorites/         # 我的收藏
│   ├── following/         # 我的关注
│   ├── followers/         # 我的粉丝
│   └── invite/            # 邀请好友
├── trade/                  # 交易订单分包
│   ├── orderList/         # 订单列表
│   ├── couponList/        # 优惠券列表
│   ├── comments/          # 评价管理
│   └── ratings/           # 评分记录
├── designer/               # 设计师相关分包
│   ├── designerAuth/      # 设计师认证
│   ├── designerOrder/     # 设计师订单
│   ├── myModels/          # 我的模型
│   └── commission/        # 佣金管理
├── security/               # 安全设置分包
│   ├── settings/          # 安全设置首页
│   ├── password/          # 修改密码
│   ├── phone/             # 手机绑定
│   └── realname/          # 实名认证
└── tools/                  # 工具类分包（低频页面）
    ├── gradient-generator/ # 渐变生成器
    ├── test1/             # 测试页面1
    └── test2/             # 测试页面2
```

## 🔄 页面迁移映射表

| 原路径 | 新路径 | 分包 | 说明 |
|--------|--------|------|------|
| `pages/index/index` | `pages/index/index` | 主包 | TabBar 页面，保持不变 |
| `pages/personalCenter/index` | `pages/personalCenter/index` | 主包 | TabBar 页面，保持不变 |
| `pages/login/index` | `pages/login/index` | 主包 | 入口页面，保持不变 |
| `pages/webview/index` | `pages/webview/index` | 主包 | 公共页面，保持不变 |
| `pages/personalCenter/profile/index` | `sub-packages/user/profile/index` | user | 用户资料 |
| `pages/personalCenter/address/index` | `sub-packages/user/address/index` | user | 地址管理 |
| `pages/personalCenter/receivingAccount/index` | `sub-packages/user/receivingAccount/index` | user | 收款账户 |
| `pages/personalCenter/points/index` | `sub-packages/user/points/index` | user | 积分中心 |
| `pages/personalCenter/favorites/index` | `sub-packages/user/favorites/index` | user | 我的收藏 |
| `pages/personalCenter/following/index` | `sub-packages/user/following/index` | user | 我的关注 |
| `pages/personalCenter/followers/index` | `sub-packages/user/followers/index` | user | 我的粉丝 |
| `pages/personalCenter/invite/index` | `sub-packages/user/invite/index` | user | 邀请好友 |
| `pages/personalCenter/orderList/index` | `sub-packages/trade/orderList/index` | trade | 订单列表 |
| `pages/personalCenter/couponList/index` | `sub-packages/trade/couponList/index` | trade | 优惠券列表 |
| `pages/personalCenter/comments/index` | `sub-packages/trade/comments/index` | trade | 评价管理 |
| `pages/personalCenter/ratings/index` | `sub-packages/trade/ratings/index` | trade | 评分记录 |
| `pages/personalCenter/designerAuth/index` | `sub-packages/designer/designerAuth/index` | designer | 设计师认证 |
| `pages/personalCenter/designerOrder/index` | `sub-packages/designer/designerOrder/index` | designer | 设计师订单 |
| `pages/personalCenter/myModels/index` | `sub-packages/designer/myModels/index` | designer | 我的模型 |
| `pages/personalCenter/commission/index` | `sub-packages/designer/commission/index` | designer | 佣金管理 |
| `pages/securitySettings/index` | `sub-packages/security/settings/index` | security | 安全设置首页 |
| `pages/securitySettings/password/index` | `sub-packages/security/password/index` | security | 修改密码 |
| `pages/securitySettings/phone/index` | `sub-packages/security/phone/index` | security | 手机绑定 |
| `pages/securitySettings/realname/index` | `sub-packages/security/realname/index` | security | 实名认证 |
| `pages/gradient-generator/index` | `sub-packages/tools/gradient-generator/index` | tools | 渐变生成器 |
| `pages/test1/index` | `sub-packages/tools/test1/index` | tools | 测试页面1 |
| `pages/test2/index` | `sub-packages/tools/test2/index` | tools | 测试页面2 |

## 🛠️ 迁移步骤

### 第一步：创建分包目录结构
```bash
# 已完成
mkdir -p src/sub-packages/{user,trade,designer,security,tools}
```

### 第二步：移动页面文件
需要将页面文件从 `src/pages/` 移动到对应的 `src/sub-packages/` 目录中。

**示例：移动用户相关页面**
```bash
# 移动个人资料页面
mv src/pages/personalCenter/profile src/sub-packages/user/

# 移动地址管理页面
mv src/pages/personalCenter/address src/sub-packages/user/

# 移动其他用户相关页面...
```

### 第三步：更新路由引用
所有跳转到分包页面的路由都需要更新路径：

**原来的跳转：**
```typescript
// ❌ 旧路径
nav.navigateTo('/pages/personalCenter/profile/index')
nav.navigateTo('/pages/securitySettings/password/index')
```

**更新后的跳转：**
```typescript
// ✅ 新路径
nav.navigateTo('/sub-packages/user/profile/index')
nav.navigateTo('/sub-packages/security/password/index')
```

### 第四步：更新组件引用
如果有组件引用了分包页面的路径，也需要更新。

### 第五步：测试验证
1. 编译检查：`pnpm build:weapp`
2. 开发者工具验证：检查分包大小和加载情况
3. 功能测试：确保所有页面跳转正常

## 📊 预期收益

### 主包体积优化
- **优化前**：所有页面都在主包，体积较大
- **优化后**：主包只有 4 个页面，体积大幅减少

### 首屏加载速度
- **TabBar 页面**：立即可用（主包）
- **用户相关页面**：进入个人中心时预下载
- **交易相关页面**：首页 WiFi 环境下预下载
- **低频页面**：按需下载

### 分包大小预估
- **主包**：< 500KB（4个页面 + 公共资源）
- **user 分包**：~300KB（8个用户相关页面）
- **trade 分包**：~200KB（4个交易相关页面）
- **designer 分包**：~250KB（4个设计师相关页面）
- **security 分包**：~150KB（4个安全设置页面）
- **tools 分包**：~100KB（3个工具页面）

## ⚠️ 注意事项

### 1. 资源引用规则
- **主包可以被分包引用**：分包可以使用主包的组件、工具函数、样式
- **分包不能互相引用**：user 分包不能直接使用 trade 分包的资源
- **公共资源放主包**：被多个分包使用的组件、工具函数应放在主包

### 2. 图片资源处理
- **小图标**：可以放在主包（< 50KB）
- **大图片**：建议上传到 CDN，通过 URL 引用
- **TabBar 图标**：必须放在主包

### 3. 路由跳转更新
- 所有页面跳转路径都需要更新
- 建议使用路由常量统一管理
- 注意 TabBar 页面路径不变

### 4. 开发调试
- 分包页面需要先进入主包页面才能调试
- 可以在开发者工具中手动进入分包页面
- 注意分包的依赖关系

## 🚀 实施建议

### 分阶段实施
1. **第一阶段**：移动 tools 分包（影响最小）
2. **第二阶段**：移动 security 分包
3. **第三阶段**：移动 designer 分包
4. **第四阶段**：移动 trade 分包
5. **第五阶段**：移动 user 分包

### 测试策略
- 每个阶段完成后进行完整测试
- 重点测试页面跳转和功能完整性
- 验证分包加载和预下载效果

这样的分包结构更符合微信小程序的最佳实践，能够显著提升用户体验和加载性能。
